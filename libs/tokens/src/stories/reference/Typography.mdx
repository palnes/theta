import { Meta } from '@storybook/addon-docs/blocks';
import { TypographyShowcase } from '../../docblocks/components/typography/TypographyShowcase';
import { TokenTable } from '../../docblocks/components';
import { TokenSystemWrapper } from '../../docblocks/components/TokenSystemWrapper';

<Meta title="Token Reference/Reference/Typography" />

# Reference Typography

Base typography values for building the type system.

<TokenSystemWrapper>

<TypographyShowcase type="reference" />

## Reference Table

<TokenTable tier="ref" filter={(token) => {
  const path = token.path.split('.');
  const category = path[1]; // ref.fontSize.16 -> fontSize
  return ['fontFamily', 'fontSize', 'fontWeight', 'lineHeight', 'lineHeightPx', 'letterSpacing', 'typography'].includes(category);
}} />

</TokenSystemWrapper>