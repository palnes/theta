import { Meta } from '@storybook/addon-docs/blocks';
import { TokenViewer, ColorRenderer } from '../../../docblocks';
import { TokenTable } from '../../../docblocks/components';
import { TokenSystemWrapper } from '../../../docblocks/components/TokenSystemWrapper';

<Meta title="Token Reference/Semantic/Colors/Action" />

# Action Colors

Colors for interactive elements like buttons, links, and form controls. These colors communicate interactivity and guide user actions.

<TokenSystemWrapper>

<TokenViewer 
  tier="sys" 
  category="color"
  renderer={new ColorRenderer()}
  showTitle={false}
  config={{
    filters: {
      include: (token) => token.path?.includes('.action.') || false
    },
    grouping: {
      enabled: false
    }
  }}
/>

## Reference Table

<TokenTable tier="sys" category="color" filter={(token) => token.path?.includes('.action.') || false} />

</TokenSystemWrapper>