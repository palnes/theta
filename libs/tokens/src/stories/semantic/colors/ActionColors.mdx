import { Meta } from '@storybook/addon-docs/blocks';
import { TokenReferenceTable } from '../../../docblocks/components/TokenReferenceTable';
import { ActionColors } from '../../../docblocks/components/ActionColors';

<Meta title="Token Reference/Semantic/Colors/Action" />

# Action Colors

Colors for interactive elements like buttons, links, and form controls. These colors communicate interactivity and guide user actions.

<ActionColors />

## Reference Table

<TokenReferenceTable tier="sys" category="color" filter={(token) => token.path.includes('.action.')} />