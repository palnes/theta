import { Meta } from '@storybook/addon-docs/blocks';
import { TokenReferenceTable } from '../../../docblocks/components/TokenReferenceTable';
import { StatusColors } from '../../../docblocks/components/StatusColors';

<Meta title="Token Reference/Semantic/Colors/Status" />

# Status Colors

Semantic colors for communicating system states, feedback, and alerts. These colors have universal meaning across the interface.

<StatusColors />

## Reference Table

<TokenReferenceTable tier="sys" category="color" filter={(token) => token.path.includes('.status.')} />