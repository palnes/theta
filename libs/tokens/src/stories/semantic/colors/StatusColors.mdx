import { Meta } from '@storybook/addon-docs/blocks';
import { TokenViewer, ColorRenderer } from '../../../docblocks';
import { TokenTable } from '../../../docblocks/components';
import { TokenSystemWrapper } from '../../../docblocks/components/TokenSystemWrapper';

<Meta title="Token Reference/Semantic/Colors/Status" />

# Status Colors

<TokenSystemWrapper>

## Info

<TokenViewer 
  tier="sys" 
  category="color"
  renderer={new ColorRenderer()}
  showTitle={false}
  config={{
    filters: {
      include: (token) => token.path?.includes('.status.info.') || false
    },
    grouping: {
      enabled: false
    }
  }}
/>

## Success

<TokenViewer 
  tier="sys" 
  category="color"
  renderer={new ColorRenderer()}
  showTitle={false}
  config={{
    filters: {
      include: (token) => token.path?.includes('.status.success.') || false
    },
    grouping: {
      enabled: false
    }
  }}
/>

## Warning

<TokenViewer 
  tier="sys" 
  category="color"
  renderer={new ColorRenderer()}
  showTitle={false}
  config={{
    filters: {
      include: (token) => token.path?.includes('.status.warning.') || false
    },
    grouping: {
      enabled: false
    }
  }}
/>

## Error

<TokenViewer 
  tier="sys" 
  category="color"
  renderer={new ColorRenderer()}
  showTitle={false}
  config={{
    filters: {
      include: (token) => token.path?.includes('.status.error.') || false
    },
    grouping: {
      enabled: false
    }
  }}
/>

## Brand

<TokenViewer 
  tier="sys" 
  category="color"
  renderer={new ColorRenderer()}
  showTitle={false}
  config={{
    filters: {
      include: (token) => token.path?.includes('.status.brand.') || false
    },
    grouping: {
      enabled: false
    }
  }}
/>

## Reference Table

<TokenTable tier="sys" category="color" filter={(token) => token.path?.includes('.status.') || false} />

</TokenSystemWrapper>