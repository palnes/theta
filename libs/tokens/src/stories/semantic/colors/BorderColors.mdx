import { Meta } from '@storybook/addon-docs/blocks';
import { TokenViewer, ColorRenderer } from '../../../docblocks';
import { TokenTable } from '../../../docblocks/components';
import { TokenSystemWrapper } from '../../../docblocks/components/TokenSystemWrapper';

<Meta title="Token Reference/Semantic/Colors/Border" />

# Border Colors

Colors for edges, dividers, and separators. These create structure and visual boundaries between elements.

<TokenSystemWrapper>

<TokenViewer 
  tier="sys" 
  category="color"
  renderer={new ColorRenderer()}
  showTitle={false}
  config={{
    filters: {
      include: (token) => token.path?.includes('.border.') || false
    },
    grouping: {
      enabled: false
    }
  }}
/>

## Reference Table

<TokenTable tier="sys" category="color" filter={(token) => token.path?.includes('.border.') || false} />

</TokenSystemWrapper>