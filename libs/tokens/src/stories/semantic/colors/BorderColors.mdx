import { Meta } from '@storybook/addon-docs/blocks';
import { TokenReferenceTable } from '../../../docblocks/components/TokenReferenceTable';
import { BorderColors } from '../../../docblocks/components/BorderColors';

<Meta title="Token Reference/Semantic/Colors/Border" />

# Border Colors

Colors for edges, dividers, and separators. These create structure and visual boundaries between elements.

<BorderColors />

## Reference Table

<TokenReferenceTable tier="sys" category="color" filter={(token) => token.path.includes('.border.')} />