import { Meta } from '@storybook/addon-docs/blocks';
import { TokenViewer, ColorRenderer } from '../../../docblocks';
import { TokenTable } from '../../../docblocks/components';
import { TokenSystemWrapper } from '../../../docblocks/components/TokenSystemWrapper';

<Meta title="Token Reference/Semantic/Colors/Text" />

# Text Colors

Colors for typography across different contexts and importance levels. These ensure readable and accessible text throughout the interface.

<TokenSystemWrapper>

<TokenViewer 
  tier="sys" 
  category="color"
  title="Text Colors"
  renderer={new ColorRenderer()}
  config={{
    filters: {
      include: (token) => token.path?.includes('.text.') || false
    },
    grouping: {
      enabled: false
    }
  }}
/>

## Reference Table

<TokenTable tier="sys" category="color" filter={(token) => token.path?.includes('.text.') || false} />

</TokenSystemWrapper>