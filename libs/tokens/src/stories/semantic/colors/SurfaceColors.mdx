import { Meta } from '@storybook/addon-docs/blocks';
import { TokenReferenceTable } from '../../../docblocks/components/TokenReferenceTable';
import { SurfaceColors } from '../../../docblocks/components/SurfaceColors';

<Meta title="Token Reference/Semantic/Colors/Surface" />

# Surface Colors

Background colors for containers, cards, and surfaces at different elevation levels. These create visual hierarchy and depth.

<SurfaceColors />

## Reference Table

<TokenReferenceTable tier="sys" category="color" filter={(token) => token.path.includes('.surface.')} />